package leetcode.linkedlist.doublylinkedlist;

public class IntroductionInsertion {
    Node constructDLL(int arr[]) {
        Node root = null;
        Node temp = null;
        Node previousNode = null;
        for (int element : arr) {
            if (root == null) {
                root = new Node(element);
                temp = root;
            } else {

            }

        }
    }

    public static void main(String[] args) {

    }
}
